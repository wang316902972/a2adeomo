# FastAPI SQL 优化审核系统依赖文件
# 基于代码分析生成的完整依赖列表
#
# 安装命令: pip install -r requirements_fastapi.txt

# ==============================
# 🚀 核心Web框架
# ==============================
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.6,<1.0.0

# ==============================
# 🤖 AI/ML框架
# ==============================
# CrewAI - SQL优化AI代理框架
crewai>= 1.4.1
crewai-tools>= 1.4.1 

# AutoGen - SQL审核AI框架 (Microsoft)
# 注意: 需要0.7+版本，与旧版本不兼容
autogen-agentchat>=0.7.0,<1.0.0
autogen-core>=0.7.0,<1.0.0
autogen-ext[openai]>=0.7.0,<1.0.0

# OpenAI API客户端
openai>=1.3.0,<2.0.0

# ==============================
# 🔧 系统工具和中间件
# ==============================
# 环境变量管理
python-dotenv>=1.0.0,<2.0.0

# CORS支持 (FastAPI内置，但某些配置可能需要)
# fastapi-cors>=0.0.6  # 注释: FastAPI内置CORSMiddleware，通常不需要额外安装

# 类型注解支持
typing-extensions>=4.8.0,<5.0.0

# HTTP客户端 (用于API调用)
httpx>=0.25.0,<1.0.0

# ==============================
# 🐳 Docker和部署支持
# ==============================
# 健康检查和监控
#curl  # 系统级依赖，Docker中使用

# ==============================
# 🧪 开发和测试依赖 (可选)
# ==============================
# pytest>=7.4.0,<8.0.0
# pytest-asyncio>=0.21.0,<1.0.0
# black>=23.0.0,<24.0.0
# flake8>=6.0.0,<7.0.0
# mypy>=1.7.0,<2.0.0

# ==============================
# 📝 说明和注意事项
# ==============================
# 1. Python版本要求: >=3.8 (推荐3.11+)
# 2. 系统要求:
#    - gcc编译器 (用于某些Python包的编译)
#    - 现代浏览器 (用于API文档访问)
# 3. 环境变量配置:
#    - OPENAI_API_KEY: OpenAI API密钥 (必需)
#    - OPENAI_BASE_URL: OpenAI API基础URL (可选)
# 4. AutoGen 0.7+ 需要Python 3.8+
# 5. 如果使用GPU加速，可能需要额外安装torch等包